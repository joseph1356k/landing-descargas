# Carpeta de descargas

Coloca aquí el archivo de la aplicación, por ejemplo `app.zip`.

El botón de descarga apunta a `downloads/app.zip`. Si tu archivo tiene otro
nombre, cámbialo en `js/config.js` (constante `DOWNLOAD_URL`).
